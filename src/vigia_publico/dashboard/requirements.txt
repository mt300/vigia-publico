# Dependencias do deploy PUBLICO (Streamlit Community Cloud), separado do
# pyproject.toml/uv.lock do projeto completo. Deliberadamente NAO inclui
# anthropic/httpx/tenacity: o dashboard publico nunca deve conseguir importar
# codigo de ingestao/analise LLM, mesmo por acidente - ver app_public.py.
#
# Streamlit Community Cloud instala via pip a partir deste arquivo (nao le
# uv.lock/pyproject.toml no formato uv). "Main file path" do app aponta pra
# src/vigia_publico/dashboard/app_public.py.
#
# NAO tem `-e .` aqui de proposito: instalar o pacote (mesmo so o proprio,
# via pip) resolveria `[project.dependencies]` inteiro do pyproject.toml da
# raiz - anthropic/httpx inclusive - o que anularia o motivo desta lista
# curada existir. app_public.py so adiciona `src/` ao sys.path (ver o inicio
# do arquivo), entao "from vigia_publico...." funciona sem pip-instalar nada
# do pacote local. Validado com um build Docker real (Dockerfile na raiz).
streamlit>=1.59.2
pandas>=3.0.3
plotly>=6.9.0
python-dotenv>=1.2.2
openpyxl>=3.1.5
